/*
 * This is the application specific customization file for the
 * core menu MainDat24AcCompMode2.
 * This only includes definitions that should apply for 'Tracking'.
 * The file is named MainDat24AcCompMode2_Tracking.menu.
*/

Menu MainDat24AcCompMode2
{
  `Marked` f.drop
  `Mark Legs` f.drop
  `Generate Report...` f.drop
  `Sort` f.drop
  
  `SubFilter` f.after
  `Sort` _o f.menu SortLegRtdAcRot
}

/* end of file */
