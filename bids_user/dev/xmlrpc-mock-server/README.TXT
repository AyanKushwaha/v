# Produce the artifact required to run the XML-RPC mock
# server by running the following command:
mvn assembly:assembly

# Look at the start.bat/start.sh to see an example of how to start the 
# XML-RPC mock server.
