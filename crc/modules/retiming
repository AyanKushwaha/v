/*
 * Root module for retiming with dummy definitions needed for Pairing 
 * reports in Rostering and PreRosterng.
 * 
 * The real retiming implementation is found in "retiming_ccp" and should
 * only be used in Pairing.
 * 
 * By Stefan Hammar in May 2012.
 *
 */

root module retiming

export %trip_penalty_retiming_use_p% = false;
export %trip_penalty_retiming_penalty% = 0;
export %total_trip_penalty_retiming% = 0;
export %num_retimed_legs% = 0;