/*  -*- crc -*- */
module base_constraints_common_definitions_apc inherits base_constraints_common_definitions

redefine export %_trip_start_date_hb% = trip.%duty_days_start_date_hb%;

redefine export %_trip_duty_days_hb% = trip.%duty_days_hb%;

redefine export %_assigned_by_name_chain%(String pos_code) =
    %bc_assigned_by_name%(pos_code);
