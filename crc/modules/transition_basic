/*-*- crc -*-*/

root module transition_basic

import levels;
import kwdmap;


export %is_course% = 
  /* Pseudo code for checking if assignment is course. Evaluates on vacations instead for now*/
  first(leg(trip), kwdmap.%activity_subtype% = "VA");